# Project wellsolve-pdf-gen

A backend service written in Go for generating PDFs from HTML for WellSolve. This application interfaces with a Gotenberg service but implements features such as CORS to restrict request origins and validate request types.

